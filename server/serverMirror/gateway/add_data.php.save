
<?php
$mysqli = new mysqli("localhost", "root", "HakunaMatatadatabase");
if ($mysqli->connect_errno) {
    die("Verbindung fehlgeschlagen: " . $mysqli->connect_error);
}
 
$sql = "UPDATE tabelle SET spalte = 'Wert' WHERE id = 1";
$mysqli->query($sql);
?>

<?php
    // Connect to MySQL
    include("connect.php");

    // Prepare the SQL statement
    $SQL = "INSERT INTO TestData VALUES ('GG120',10,20,30)";     

    // Execute SQL statement
    mysql_query($SQL);
?>
